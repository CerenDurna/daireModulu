const circleArea = require('./circle.js'); // Fonksiyonu bir değişkene atıyoruz.


circleArea.showCircleArea(5)// Kendi değişkenimiz üzerinden çalıştırıyoruz. 
circleArea.circleAreashowCircleCircumference(5);

//destructing
const { showCircleArea, showCircleCircumference } = require('./circle.js');

circleArea.showCircleArea(5)// Kendi değişkenimiz üzerinden çalıştırıyoruz. 
circleArea.circleAreashowCircleCircumference(5);
